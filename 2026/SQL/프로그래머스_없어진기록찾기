-- from은 select문 이전에 실행된다.
SELECT M.ANIMAL_ID ANIMAL_ID, M.NAME NAME	
from (SELECT O.ANIMAL_ID ANIMAL_ID,O.NAME NAME,I.ANIMAL_ID IID
FROM ANIMAL_OUTS O left  join ANIMAL_INS I on O.ANIMAL_ID=I.ANIMAL_ID) M
where M.IID IS NULL
ORDER BY ANIMAL_ID